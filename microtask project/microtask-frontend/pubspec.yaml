name: microtask_project   # <-- Add this at the very top, no quotes needed
description: A new Flutter project.
publish_to: "none"       # optional, prevents publishing to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  http: ^1.1.0
  razorpay_flutter: ^1.4.1
  file_picker: ^8.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/home_bg.jpg
    - assets/login_bg.jpg
